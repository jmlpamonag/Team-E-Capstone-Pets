<div class="body-content">


	<h2 style="background-color: white;color: black; text-align: center; font-style: oblique">Report Of Visit Reasons Offered</h2>


	@Html.ActionLink("Create New", "Save", "TVisitReasons", null, new { @style = "font-size:22px;", @class = "popup" })



	<div id="update_panel">
		@* This will be formatted and pulled from the JS file. *@
	</div>

	@* Add Jquery UI Css *@
	<link rel="stylesheet" href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css" />

	<style>
		table {
			font-family: arial, sans-serif;
			border-collapse: collapse;
			width: 100%;
		}

		td, th {
			border: 1px solid #dddddd;
			text-align: left;
			padding: 8px;
		}

		tr:nth-child(even) {
			background-color: #dddddd;
		}

		.ui-widget-header {
			border: none !important;
			background: none !important;
			color: #222222;
			font-weight: bold;
		}

		.ui-state-default, .ui-state-hover {
			border: none !important;
			background: none !important;
		}

		.ui-dialog .ui-dialog-buttonpane .ui-dialog-buttonset {
			float: none;
		}

		.ui-dialog {
			webkit-box-shadow: 0 5px 15px rgba(0,0,0,.5);
			box-shadow: 0 5px 15px rgba(0,0,0,.5);
		}

		.ui-dialog-buttonset {
			position: inherit;
		}

		h2 {
			margin-top: 0px;
			font-size: 30px;
		}
	</style>
</div>

	@section Scripts{
		<script src="//code.jquery.com/ui/1.11.4/jquery-ui.js"></script>
		<script src="~/Scripts/VisitReasons.js"></script>
	}










	@*@{
			ViewBag.Title = "Index";
		}

		<h2>Index</h2>

		<p>
			@Html.ActionLink("Create New", "Create")
		</p>

		<table class="table">
			<tr>
				<th>
					@Html.DisplayNameFor(model => model.strVisitReason)
				</th>
				<th></th>
			</tr>
			@foreach (var item in Model) {
				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.strVisitReason)
					</td>

					<td>
						@Html.ActionLink("Edit", "Edit", new { id = item.intVisitReasonID }) |
						@Html.ActionLink("Details", "Details", new { id = item.intVisitReasonID }) |
						@Html.ActionLink("Delete", "Delete", new { id = item.intVisitReasonID })
					</td>
				</tr>

			}

		</table>

		@using (Html.BeginForm("Export", "TVisitReasons", FormMethod.Post)) {
			<input type="hidden" name="ExportData" />
			<input type="submit" id="btnSubmit" value="Export" />
		}


		<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
		<script type="text/javascript">
			$(function () {
				$("#btnSubmit").click(function (event) {
					var target = $(event.currentTarget).data('row');
					$("input[name='ExportData']").val($("PrintPDF").html());
				});
			});*@
	@*</script>*@
